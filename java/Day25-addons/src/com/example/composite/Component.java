package com.example.composite;

/**
 * The Component interface declares a common operation
 * for both simple (Leaf) and complex (Composite) objects.
 */
public interface Component {
    void brandName();
}
